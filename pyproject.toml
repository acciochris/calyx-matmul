[project]
name = "calyx-matmul"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "calyx-ast",
]

[tool.uv.sources]
calyx-ast = { path = "../calyx/calyx-py" }
